---
name: investment-evaluation
version: 2.0
description: 新能源项目（风电/光伏/储能/算力中心）投资价值评估与风险分析。触发词：投资评估、项目评估、投资分析、新能源投资、风电投资、光伏投资、储能投资、算力中心投资、投资尽调、项目尽调、财务评估。输出：Markdown报告 + Word报告 + 双风格PPT（投行研报 + 路演）。内置 wind-power-evaluation-claude 子技能用于风电财务测算。v2.0 新增：双风格PPT生成（投行研报+路演）、Word格式与wind-power-evaluation-claude对齐、所有输出文件自动同步 Obsidian wiki/topics/evaluations/、程序化视觉QA。
---

# 投资评估技能 (Investment Evaluation Skill)

新能源项目（风电/光伏/储能/算力中心）投资价值评估与风险分析。风电项目自动调用内嵌的 `wind-power-evaluation-claude` 子技能执行财务测算。

## 触发词

- 「投资评估」「项目评估」「投资分析」「项目分析」
- 「评估这个项目」「分析这个投资」
- 「新能源投资」「风电投资」「光伏投资」「储能投资」
- 「算力中心投资」「数据中心投资」
- 「投资尽调」「项目尽调」「财务评估」
- 「股权直投评估」「基金GP评估」「EPC评估」

## 支持的参与模式

| 模式 | 说明 | 输出 |
| --- | --- | --- |
| 股权直投 | 作为股东直接投资，分享股权收益 | 全投资IRR、资本金IRR、NPV |
| 股权基金GP+基石投资人 | 担任GP管理角色+基石出资 | 基金IRR、GP收益、管理费/Carry结构 |
| EPC参与 | 作为工程承包商承接项目 | EPC利润、工程利润率 |

---

## 评估流程

### Phase 0: 模式确认

询问用户参与角色（股权/基金GP/EPC），只计算适用的模式。风电项目默认进入 Phase 2 子技能调用。

---

### Phase 1: 数据解析

**Task 1 — 从项目文件夹批量提取数据**

- 读 docx：`python3 -c "from docx import Document; doc = Document('path'); print([p.text for p in doc.paragraphs if p.text.strip()])"`
- 读 xlsx：`load_workbook(path, data_only=True)` 逐行输出
- 读 pdf：用 pymupdf（`/opt/homebrew/bin/python3 -c "import fitz..."`）提取文字
- 读合同 → 输出关键部分（合作模式、费用、风险条款）
- 参考：`references/multi-source-data-extraction.md`

**Task 2 — 数据交叉验证与一致性检查**

- 不同来源的相同参数可能不一致，将所有取值列在一个表中比对
- 发现差异时标注为"需确认"项

**Task 3 — 建立项目参数表**

- 整理关键参数为结构化表格，标注来源和置信度
- 使用 `templates/parameters-template.md`

---

### Phase 2: 风电项目财务测算（子技能调用）

> ⚠️ 对于风电项目，本阶段**必须委托** `wind-power-evaluation-claude` 子技能执行完整的财务测算。 子技能路径：`skills/wind-power-evaluation-claude/`

#### 子技能执行流程

`wind-power-evaluation-claude` 按以下步骤执行：

**第零步：前置准备**

- 搜索 Obsidian 知识库（`entities/`、`concepts/`、`topics/`）获取项目基本信息
- 硬性限制：只提取项目所在地和装机容量；电价、利用小时数、投资概算**禁止**从知识库提取，必须通过独立搜索获取
- 处理用户上传的 PDF 数据文件（pymupdf 提取文字）
- 获取省级规划文件（`concepts/{省份}新能源规划.md` + `concepts/{省份}电网规划.md`）

**第一步：获取项目信息 + 利用小时数**

- 询问项目所在地、装机容量、接入方案
- 利用小时数分两步：理论值（多源搜索取低值输入模型）+ 实际值（用于校核限电率）
- 硬性要求：通过 Tavily API 搜索（`curl -s https://api.tavily.com/search`），API Key: `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`，每类 ≥10 个来源
- 🔴 铁律：**禁止使用浏览器搜索**，只用 Tavily API

**第二步：电价获取**

- 搜索机制电价、双边交易价、现货价三类，每类 ≥10 个来源
- 必须找到该省 136 号文实施细则，提取六大核心参数：竞价上限、实际中标价、机制电量比例、中标机制电量、执行期限、基准利用小时
- 关键陷阱：机制电量的基数可能是省定基准小时（如新疆 895h）而非项目实际小时，两者差异可达 300%+
- 六种机制电价参数陷阱（mlt_ratio≠1.0、全生命周期不要统一电价、计算口径必须查文件、混淆申报和中标比例、扣费前后混淆、现货季节偏差）

**第三步：确定关键参数**

- 限电率双轨道估算（轨道A：项目所在地 + 轨道B：全省均值），取最高值输入模型
- 电价策略：情况一（不参与竞价）或 情况二（参与竞价，二阶段电价机制期+市场化期）
- 财务参数：经营期 20 年、融资利率 4%、融资期限 18 年等
- 运维费分段：1-5年 0.02、6-10年 0.06、11-20年 0.08 元/W·年

**第四步：执行核心测算**

- 使用 `skills/wind-power-evaluation-claude/scripts/financial_model.py` 执行
- 任务1（投资边界）：100% 融资，最小 DSCR ≥ 1.2
- 任务4（出售边界）：80% 融资，全投资 IRR ≥ 6% 且资本金 IRR ≥ 8%
- 注意 search_lo 陷阱：有效电价 &lt; 0.25 时设 1.0，0.25-0.35 时设 1.5，&gt; 0.35 时设 2.5

**第五步：输出结果**

- 必选A：电价政策溯源（10 项，含 URL）
- 必选B：五步电价推导（从原始参数到财务输入）
- 必选C：13 行多口径电价表（含税/不含税/扣费前后）
- 必选D：现货电价三情景敏感性分析
- 六、财务测算结果（模型边界条件 + 两任务结果）
- 七、投资决策建议

**第六步：保存报告**

- 双备份：`projects/` + Obsidian `wiki/topics/evaluations/`
- 报告文件（.md + .docx）+ PPT 文件（.pptx）全部同步
- 更新 Obsidian 实体页 frontmatter、正文、PPT 文件引用

**子技能输出数据**（传递给后续 Phase）：

| 数据 | 用途 |
| --- | --- |
| 机制电价 / 机制电量比例 / 执行期限 | Phase 3 政策风险评估、Phase 5 敏感性分析 |
| 限电率（轨道A/B/C 三值 + 最终取值） | Phase 3 市场风险评分 |
| 出售边界 / 投资边界 (元/W) | Phase 5 敏感性分析基准 |
| 全投资IRR / 资本金IRR / DSCR | Phase 3 现金流风险评分 |
| 年度利润表 / 现金流表 / DSCR 表 | 报告附表 |
| 接入方案 + 汇集站状态 | Phase 3 建设风险评估 |
| 交易结构（基金/皖能） | Phase 4 合作方尽调 |

---

### Phase 3: 风险评估

**Task 7 — 六类风险评级**

- 市场/技术/政策/现金流/建设/商务风险，每类评分 1-5
- 使用 `templates/risk-matrix-template.md`
- 引用 Phase 2 输出的限电率、电价、DSCR 等数据作为评分依据

**Task 8 — 合作方尽调**

- 每个合作方单独评估（信誉/资信/实力/配合度/利益冲突）
- GP 角色专属：控制权架构/Carry 分配/管理费/投委会席位
- 使用 `templates/partner-assessment-template.md`

---

### Phase 4: 综合建议

**Task 10 — 综合建议**

- 是否参与建议（强烈推荐/推荐/中性/不推荐）
- 参与方式建议、风险提示、后续行动清单

---

### Phase 5: 敏感性分析

**Task 11 — 关键参数敏感性**

- 股权模式：电价、发电量、建设成本、利率 → 引用 Phase 2 财务模型结果
- 参照基准 ±10%/±20% 变化
- 参考：`references/industry-benchmarks.md`

**Task 12 — 情景分析**

- 乐观/基准/悲观三种情景，IRR 和 NPV 对比，盈亏平衡条件分析

---

### Phase 6: 报告生成

> ⚠️ **硬性规则**：报告生成前必须先加载 `templates/` 下的模板文件。

#### 输出结构（合并后统一格式）

**不再输出 6 份独立 Markdown 文件**，改为输出以下 4 份综合文件：

| 序号 | 文件 | 格式 | 内容 |
| --- | --- | --- | --- |
| 1 | `{项目名}_{容量}MW风电项目投资评估报告_{YYYYMMDD}.docx` | Word | 全 11 章 + 附表 + Logo 页眉 |
| 2 | `{项目名}_{容量}MW风电项目投资评估报告_{YYYYMMDD}.md` | Markdown | 全 11 章 + 附表（与 Word 内容一致） |
| 3 | `ppt/{项目名}_投行研报.pptx` | PPT | 风格A：投行研报 12 页 |
| 4 | `ppt/{项目名}_路演.pptx` | PPT | 风格B：路演 Pitch Deck 12 页 |

> ⚠️ **硬性规则：所有文件双备份到 Obsidian**。4 份文件全部同步到 `wiki/topics/evaluations/{项目名}/`。

```bash
# 报告 + PPT 同步到 Obsidian（生成后立即执行）
OBSIDIAN="{obsidian根目录}/wiki/topics/evaluations/{项目名}"
mkdir -p "$OBSIDIAN"
cp {项目名}_{容量}MW风电项目投资评估报告_{YYYYMMDD}.docx "$OBSIDIAN/"
cp {项目名}_{容量}MW风电项目投资评估报告_{YYYYMMDD}.md "$OBSIDIAN/"
cp ppt/{项目名}_投行研报.pptx "$OBSIDIAN/"
cp ppt/{项目名}_路演.pptx "$OBSIDIAN/"
```

**Obsidian 实体页更新**（`entities/{项目名}.md`）：在正文末尾追加评估文件嵌入引用 + PPT 文件链接。

**报告包含的 11 章**：

 1. 执行摘要（含关键指标总览 + 风险等级）
 2. 项目概况（基本信息 + 交易结构 + 接入设施）
 3. 电力市场与电价分析 \[必选A-D\]（10 项政策溯源 + 五步推导 + 13 行口径表 + 三情景敏感性）
 4. 限电率评估（双轨道 + 三者取最高 + 可研分段制）
 5. 财务测算结果（模型边界 + 出售边界 + 基金场景）
 6. 敏感性分析（收购价扫描 + 核心变量敏感度排名）
 7. 风险评估矩阵（六类评分 + 热力图 + 缓释措施）
 8. 合作方尽调（合作方一览 + 各维度评估）
 9. 投资决策建议（参与建议 + 推荐价格 + 投资条件 + 红线指标）
10. 财务指标附表（利润表 + 现金流表 + DSCR 表）
11. 免责声明（七段原文）

---

### Phase 7: PPT 生成

> ⚠️ **硬性规则**：
>
> 1. PPT 生成在 Word + Markdown 报告完成之后执行
> 2. **必须使用** `/pptx` **技能**（`proma-workspace-default:pptx`）来生成 PPT，不可手写 pptxgenjs
> 3. **两种风格都必须输出**，不可只输出一种
> 4. 每次调用 `/pptx` 时，将下面风格 A/B 的提示词完整传递给技能

---

#### 调用方式

```
# 风格 A · 投行研报
Skill("proma-workspace-default:pptx", args="[风格A的完整提示词+项目数据]")

# 风格 B · 路演  
Skill("proma-workspace-default:pptx", args="[风格B的完整提示词+项目数据]")
```

**两个调用串行执行**（A 完成后启动 B），`.pptx` 输出到 `ppt/` 子目录。`/pptx` 技能自带 QA 流程（markitdown → PDF → 图片 → 视觉检查），生成后无需重复 QA。

---

#### 共用配色（注入到提示词中）

```
配色方案 Midnight Executive（两风格统一）：
- 主背景：#FFFFFF / #F8FAFC（卡片底色）
- 深色页：#1A3A5C（封面/章节页） / #0F2440（收尾页）
- 主文字：#1E293B（深灰正文） / #475569（中灰辅助）
- 强调色：#1A3A5C（标题/Key Insight 统一用此色，不可用橙色）
- 冰蓝色 #D6E4F0 仅用于深蓝背景上的文字，白色背景上的所有文字用 #1E293B 或 #475569
- 卡片底色：#F1F5F9 / 图表色：#2C5F8A
- 正向绿：#059669 / 警告橙：#D97706（仅数值标注）/ 风险红：#DC2626
- 边框：#E2E8F0 / 页脚：#94A3B8
- 字体：Arial（标题）+ Calibri（正文），16:9 宽屏
- 表格：表头深蓝背景 #1A3A5C + 白字，数据行交替底色
- 白色文字 #FFFFFF 仅允许出现在深蓝背景上
```

**🔴 三个配色铁律（注入到每个提示词末尾）**：

1. "Key Insight / 行为标题统一用深蓝色 #1A3A5C 斜体 11-13pt，不可用橙色"
2. "冰蓝色 #D6E4F0 仅用于深蓝背景上的文字，白色背景上的所有文字使用 #1E293B 或 #475569"
3. "白色文字 #FFFFFF 仅允许出现在深蓝 #1A3A5C 或 #0F2440 背景上"

---

#### 风格 A · 投行研报 — 传递给 `/pptx` 的提示词

```
制作一份关于《{项目完整名称} 深度分析与投资建议》的专业投行研报 PPT。

**风格**：极简投研风（Research Style）。白/浅灰底色，主色深海蓝 + 金融灰。禁止无意义装饰。高信息密度但结构化——逻辑树、矩阵图、流程图。图表精准：面积图/折线图（趋势）、散点图（对标）、数据表格（电价/财务）、柱状图（敏感性）。

**配色**：{粘贴上面的"共用配色"块}

**数据**：以下数据来自已完成评估报告，直接使用，不重新搜索：
{粘贴 Phase 2 核心数据：项目名、装机、利用小时、限电率、机制电价/MLT价/绿电价、有效电价、出售边界、投资边界、全投IRR、资本金IRR、DSCR、LCOE、年均净现金流、敏感性分析表、风险列表、投资结论}

**内容要求**：每页顶部"Key Insight"用深蓝 #1A3A5C 斜体。禁止口水话，所有定性结论有定量数据支撑，数据到小数点后两位。

**12 页结构**：
1. 封面："{项目名} 深度分析与投资建议" / 副标题 / "新能源·风电" / "Proma Agent · {日期}"。深蓝背景+白字
2. 投资摘要：3-4条核心逻辑（≤30字/条），左卡逻辑条目+右深蓝卡核心指标大字报
3. 宏观与政策环境：政策时间线+市场数据卡片。标注政策文件出处
4. 供需格局与趋势：核心指标复盘+预测，附面积图/折线图
5. 标的资产深度拆解：核心参数表（所在地/装机/利用小时/限电率/年发电量/并网时间）
6. 横向对标：2-3个可比标的五维对比表，高亮本项目列
7. 商业模式与降本路径：交易结构流程图（基金GP+预收购）
8. 核心财务假设：收入/成本/融资参数表格
9. 收益测算模型：全投IRR/资本金IRR/NPV/DSCR/LCOE KPI塔+柱状图
10. 敏感性分析：机制比例+MLT电价双维度，双柱状图
11. 风险提示：4列风险卡片（描述+应对策略）
12. 结论与投资建议：投决建议+收购区间+红线，深蓝背景收束

**每页底部**：页码（NN/12，8pt 浅灰）+ Source 标注。
```

---

#### 风格 B · 路演 — 传递给 `/pptx` 的提示词

```
制作一份关于《{项目完整名称} 投资价值与可行性深度分析》的路演级别 PPT。

**风格**：Pitch Deck，视觉冲击力+宏大叙事。大图留白，核心指标极大加粗字体突出。高对比度极简图表。"一页一结论"，每页顶部"行为标题"直接给结论。配色与风格A统一（Midnight Executive 白底深蓝）。

**配色**：{粘贴上面的"共用配色"块}

**数据**：{粘贴与风格A相同的 Phase 2 核心数据}

**12 页结构**：
1. 封面：极具冲击力标题 + 投资人副标题（如"在电力市场化改革中寻找确定性超额收益"）
2. 投资亮点 The Hook：3个核心结论大字号，每个配超大数字+一行解释
3. 时代机遇：左大字报数据+右简短叙事
4. 直面痛点：左右对比——左"问题"红色数字 + 右"解决方案"绿色数字
5. 核心壁垒 Unfair Advantage：4列卡片（编号/标签/标题/描述）
6. 破局催化 The Catalyst：3行催化因素（标题+描述+量化指标）
7. 降维打击 Benchmarking：极简五维对比表，高亮本项目
8. 盈利密码：三步流程图+最终电价大字报
9. 财务回报 The Money Shot：超大收购价数字+6格指标矩阵
10. 投资决策 Call to Action：绿色"建议放行"+推荐区间+4条红线
11. 风险对冲：4列风险卡片（精简版）
12. Q&A+数据溯源

**每页正文≤60字，数据到小数后两位，每页底部署名+Source。**
```

---

#### 生成后 QA

`/pptx` 技能自带 QA 流程。额外补充：对两个生成的 `.pptx` 分别运行自动化对比度检查脚本（见 `references/qa_pptx.py`），确认无低对比度/橙色 Key Insight 问题后交付。

## Word 报告格式规范

### 页面布局

| 参数 | 数值 |
| --- | --- |
| 纸张 | A4 (21cm × 29.7cm) |
| 上边距 | 2.5cm |
| 下边距 | 2.2cm |
| 左边距 | 2.5cm |
| 右边距 | 2.5cm |
| 页码 | 不标注页码 |

### 字体层级

| 层级 | 字体 | 字号 | 颜色 | 加粗 |
| --- | --- | --- | --- | --- |
| 文档标题（H1） | 黑体 | 15pt | #1B3A5C | ✅ |
| 章节标题（H2） | 黑体 | 13pt | #1B3A5C | ✅ |
| 小节标题（H3） | 黑体 | 11pt | #1B3A5C | ✅ |
| 正文 | 仿宋 | 11pt | 黑色 | ❌ |
| 表格表头 | 黑体 | 9pt | #FFFFFF 白字 + #1B3A5C 深蓝背景 | ✅ |
| 表格内容 | 仿宋 | 9pt | 黑色（交替行底色 #F2F2F2） | ❌ |

### 关键代码要求

- **必须设置** `w:eastAsia` **属性**：`run._element.rPr.rFonts.set(qn('w:eastAsia'), '黑体')`，否则中文字体回退
- **Logo 页眉**：用户提供的 Logo 文件嵌入每页右上角，宽度 2cm
- **免责声明**：七段原文，一字不改
- **表格**：Table Grid 边框，表头深蓝背景 + 白字，数据行交替底色
- **python-docx 环境**：使用 `/opt/homebrew/bin/python3`（已安装 python-docx）

### 字体实现代码（固化）

```python
from docx.oxml.ns import qn
from docx.oxml import OxmlElement

def set_font(run, name, size, bold=False, color=BK):
    run.font.name = name
    run.font.size = size
    run.bold = bold
    run.font.color.rgb = color
    rPr = run._element.get_or_add_rPr()
    rFonts = rPr.find(qn('w:rFonts'))
    if rFonts is None:
        rFonts = OxmlElement('w:rFonts')
        rPr.insert(0, rFonts)
    rFonts.set(qn('w:eastAsia'), name)
```

### 免责声明模板

```
本报告由江能研究院（基于 AI 大语言模型辅助）生成，仅供项目投资决策参考，不构成任何形式的投资建议或承诺。

报告中所引用的电力市场数据均来自公开渠道，部分电价参数在缺乏直接交易数据的情况下采用保守假设推算，可能与实际成交价格存在偏差。

实际项目投资决策应结合以下因素综合判断：
① 场址实测测风数据（至少一个完整年度）；② 电网接入批复及送出工程条件；③ EPC 招标实际报价；④ 项目所在地最新的机制电价竞价结果；⑤ 所在省电力交易中心公布的全年现货与中长期交易结算数据。

本报告中的财务模型基于特定假设（融资利率 4%、经营期 20 年、等额本金还款等），不同融资结构、利率环境及政策变化可能导致测算结果显著偏离。

报告中的"投资边界"和"出售边界"为理论测算阈值，不代表项目实际可实现的交易价格或融资条件。

江能能源及报告编制方不对因使用本报告而产生的任何直接或间接损失承担责任。未经授权，不得转载或用于商业用途。
```

---

## 参考文件

| 路径 | 说明 |
| --- | --- |
| `templates/parameters-template.md` | 项目参数表模板 |
| `templates/risk-matrix-template.md` | 六类风险评分模板 |
| `templates/partner-assessment-template.md` | 合作方尽调模板 |
| `templates/report-template.md` | 报告结构模板 |
| `references/financial-formulas.md` | 财务公式参考 |
| `references/industry-benchmarks.md` | 行业基准数据 |
| `references/epc-modeling.md` | EPC 建模参考 |
| `references/multi-source-data-extraction.md` | 多源数据提取指南 |
| `../wind-power-evaluation-claude/scripts/financial_model.py` | 风电财务模型（子技能） |
| `../wind-power-evaluation-claude/SKILL.md` | 风电评估子技能完整文档 |

---

## Pitfalls

- **数据一致性**：Phase 2 输出的数据是后续所有 Phase 的唯一真源，不得重复计算或手动填写
- **模板先于输出**：不要在加载模板之前输出报告
- **参与模式选择**：先确认用户的参与角色，只计算适用的模式
- **置信度标注**：所有数据必须标注置信度（高/中/低）
- **PPT 两种风格都输出**：投行研报版和路演版都必须生成
- **PPT 字体**：使用 CJK 兼容字体（Microsoft YaHei / Arial），不用 Georgia（无 CJK glyph）
- **输出文件**：统一为单 Word + 单 MD + 双 PPT，不输出 6 份独立 MD
- **子技能隔离**：风电项目的电价、限电率、利用小时数搜索由子技能独立完成，不在主技能中重复搜索
- **python-docx 环境**：必须使用 `/opt/homebrew/bin/python3`，不是 sandbox Python

---

## 注意事项

- 所有输出使用中文
- 财务公式必须数学正确
- 风电项目的参数搜索**禁止使用浏览器**，只用 Tavily API
- 存在不确定性时采用保守假设（电价取低、限电率取高、利用小时取低）
- 所有引用数据需注明来源